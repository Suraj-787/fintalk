# FinTalk Environment Variables

# Backend API Configuration
# Set this to your FastAPI backend URL (default: http://localhost:8000)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Note: Google and Sarvam API keys are managed through the Settings page in the app
# They are stored locally in the browser's localStorage and never sent to the server
